import { StyleSheet, Text, View } from "react-native";

const Slider = ()=>{
    return (
        <View>
            <Text>Slider</Text>
        </View>
    )
};

export default Slider;

const slider_styles = StyleSheet.create({});